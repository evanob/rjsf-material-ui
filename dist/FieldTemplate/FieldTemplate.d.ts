/// <reference types="react" />
import { FieldTemplateProps } from 'react-jsonschema-form';
declare const FieldTemplate: ({
  id,
  children,
  displayLabel,
  rawErrors,
  rawHelp,
  rawDescription,
  formContext,
}: FieldTemplateProps) => JSX.Element;
export default FieldTemplate;
