/// <reference types="react" />
import { WidgetProps } from 'react-jsonschema-form';
declare const RangeWidget: ({
  value,
  readonly,
  disabled,
  onBlur,
  onFocus,
  options,
  schema,
  formContext,
  onChange,
  required,
  label,
  id,
}: WidgetProps) => JSX.Element;
export default RangeWidget;
