/// <reference types="react" />
declare const SelectWidget: ({
  schema,
  id,
  options,
  label,
  required,
  disabled,
  readonly,
  value,
  multiple,
  autofocus,
  onChange,
  onBlur,
  onFocus,
  formContext,
}: any) => JSX.Element;
export default SelectWidget;
