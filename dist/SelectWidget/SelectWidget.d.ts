/// <reference types="react" />
declare const SelectWidget: ({
  schema,
  id,
  options,
  label,
  required,
  disabled,
  readonly,
  value,
  multiple,
  autofocus,
  onChange,
  onFocus,
  onBlur,
  formContext,
}: any) => JSX.Element;
export default SelectWidget;
